algoritmo "operadores"
var
   a, b, c, d: real
inicio
      // + soma, - subtração, * multiplicação, / divisão, \ div. inteira, ^ exponenciação, % resto div.
      // abs, exp, int, raizq, pi, sen, cos, tan, graupRad

      a <- Abs(-9)
      b <- Exp(3,2)
      c <- RaizQ(81)
      d <- Int(RaizQ(90))
      
      escreva(a, b, c, d)
fimalgoritmo